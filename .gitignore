__pycache__/
*.pyc
~$*.xlsx

# Generated/derived data — rebuilt by step 4 & 5, so don't sync these
# (avoids large-file merge conflicts across computers; regenerate locally)
sortpin.db
sortpin_data.json
sortpin_viewer.html

# NOTE: the raw SortPin CSV exports stay tracked (shared source data):
#   SortPin.com_all_leads_*.csv / _boards_*.csv / _pins_*.csv
